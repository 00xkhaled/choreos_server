image: maven:3-jdk-11

stages:
  - test
  - build
  - deploy

test:
  stage: test
  script: mvn test

build:
  stage: build
  script: mvn install -B

deploy_staging:
  stage: deploy
  script:
    -  cd /var/wwww
    - git pull
  environment:
    name: staging
    url: https://staging.example.com
  only:
    - master
